# Frontend Application

Веб-приложение для игры "The Last of Guss" с архитектурой на основе Web Components.

## 🎯 Обзор

Frontend часть монопольного репозитория, реализованная как современное SPA (Single Page Application) с использованием нативных Web Components и модульной архитектуры.

## 🏗️ Архитектура

### Базовая структура
```
src/
├── base/                 # Базовые классы и утилиты
├── components/           # Компоненты приложения
├── modules/             # Вспомогательные модули
└── tests/               # Тесты
```

### Ключевые компоненты

#### 🎮 Основные компоненты
- **`navigation-manager`** - Управление навигацией и состоянием приложения
- **`state-manager`** - Централизованное управление состоянием
- **`api-service`** - Сервис для взаимодействия с API
- **`login-page`** - Страница авторизации
- **`rounds-list`** - Список игровых раундов
- **`round-page`** - Страница отдельного раунда

#### 🎨 Базовый класс
- **`BaseComponent`** - Абстрактный базовый класс для всех компонентов
  - Жизненный цикл компонентов
  - Система рендеринга и шаблонов
  - Управление состоянием
  - Обработка ошибок
  - Модальные окна

## 🚀 Функциональность

### Авторизация
- Вход в систему
- Управление сессиями
- Защита маршрутов

### Игровые раунды
- Просмотр списка раундов
- Создание новых раундов (для администраторов)
- Участие в активных раундах
- Тап-механика с системой очков
- Специальные тапы (каждый 11-й тап = 10 очков)

### Навигация
- Динамическая маршрутизация
- Управление историей
- Защищенные маршруты

## 🛠️ Технологии

- **Web Components** - Нативные веб-компоненты
- **ES Modules** - Модульная система
- **CSS Variables** - Динамические стили
- **Shadow DOM** - Изоляция стилей
- **Custom Elements** - Пользовательские элементы

## 🎨 Стилизация

### Дизайн-система
- CSS переменные для темной/светлой темы
- Адаптивный дизайн
- Анимации и переходы
- Скелетон-загрузка

### Темы
- Светлая тема (по умолчанию)
- Темная тема (автоматическое переключение)

## 📦 Установка и запуск

### Требования
- Node.js 16+
- npm/yarn

### Установка
```bash
# Из корня монопольного репозитория
npm install

# Сборка frontend
npm run build:frontend

# Запуск в development режиме
npm run dev
```

### Скрипты
```bash
# Сборка frontend
npm run build:frontend

# Запуск proxy сервера
npm run proxy
```

## 🔧 Разработка

### Структура компонента
Каждый компонент следует стандартной структуре:
```
component-name/
├── actions/        # Бизнес-логика
├── controller/     # Обработчики событий
├── css/           # Стили компонента
├── template/      # Шаблоны рендеринга
└── index.mjs      # Основной файл компонента
```

### Создание нового компонента
1. Наследоваться от `BaseComponent`
2. Реализовать необходимые методы жизненного цикла
3. Определить шаблоны и стили
4. Зарегистрировать кастомный элемент

### Пример компонента
```javascript
import { BaseComponent } from '../../base/base-component.mjs';

export class MyComponent extends BaseComponent {
    constructor() {
        super();
        this.state = { /* состояние компонента */ };
    }
    
    async _componentReady() {
        await this.fullRender(this.state);
    }
}

customElements.define('my-component', MyComponent);
```

## 🧪 Тестирование

### Запуск тестов
```bash
# Тесты находятся в директории tests/
npm test
```

### Структура тестов
- Модульные тесты компонентов
- Интеграционные тесты взаимодействия
- E2E тесты критических путей

## 🔄 Состояние и данные

### Управление состоянием
- Локальное состояние в компонентах
- Глобальное состояние через `state-manager`
- Синхронизация с API

### API взаимодействие
- RESTful API через `api-service`
- Автоматическая обработка токенов
- Обработка ошибок

## 🎮 Игровая механика

### Система очков
- Обычный тап: 1 очко
- Каждый 11-й тап: 10 очков
- Роль "nikita": очки не начисляются

### Время раундов
- Cooldown: подготовка к раунду
- Active: активная фаза игры
- Finished: завершенный раунд

## 📱 Адаптивность

### Поддержка устройств
- Десктоп (1200px+)
- Планшеты (768px - 1199px)
- Мобильные (< 768px)

### Особенности
- Touch-жесты для мобильных устройств
- Адаптивная типографика
- Оптимизированные интерфейсы

## 🔒 Безопасность

### Защита
- JWT аутентификация
- Валидация входных данных
- Защита от XSS через Shadow DOM
- HTTPS обязателен для production

## 🚀 Производительность

### Оптимизации
- Ленивая загрузка компонентов
- Кэширование API запросов
- Оптимизированные анимации
- Минификация и сжатие

### Мониторинг
- Логирование действий
- Отслеживание ошибок
- Метрики производительности