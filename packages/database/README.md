# The Last of Guss - Database Package

Монопакет базы данных для проекта "The Last of Guss". Содержит модели, конфигурацию и утилиты для работы с PostgreSQL через Sequelize ORM.

## 📦 Структура пакета

```
database/
├── src/
│   ├── config/
│   │   └── database.ts      # Конфигурация подключения к БД
│   ├── models/
│   │   ├── User.ts          # Модель пользователя
│   │   ├── Round.ts         # Модель раунда игры
│   │   ├── Participant.ts   # Модель участника раунда
│   │   └── index.ts         # Экспорт всех моделей
│   ├── types/
│   │   └── index.ts         # TypeScript типы и интерфейсы
│   └── index.ts             # Главный файл экспорта
├── scripts/
│   └── build.js             # Скрипт сборки
└── package.json
```

## 🎯 Модели данных

### User (Пользователь)
- `id` (UUID) - уникальный идентификатор
- `username` (string) - имя пользователя (уникальное)
- `password` (string) - хешированный пароль
- `role` (enum) - роль: 'user', 'admin', 'nikita'
- Автоматическое хеширование пароля при создании/обновлении
- Автоматическое определение роли по имени пользователя

### Round (Раунд)
- `id` (UUID) - уникальный идентификатор
- `start_date` (Date) - начало раунда
- `end_date` (Date) - окончание раунда
- `total_score` (number) - общий счет раунда
- `status` (enum) - статус: 'cooldown', 'active', 'finished'
- Автоматическое определение статуса на основе дат

### Participant (Участник)
- `id` (UUID) - уникальный идентификатор
- `user_id` (UUID) - ссылка на пользователя
- `round_id` (UUID) - ссылка на раунд
- `tap_count` (number) - количество тапов
- `score` (number) - набранные очки
- Уникальная связка user_id + round_id

## 🔧 Установка и настройка

### Требования
- Node.js 18+
- PostgreSQL 12+
- TypeScript 5.0+

### Установка зависимостей
```bash
npm install
```

### Настройка окружения
Создайте файл `.env` в корне проекта:
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=the_last_of_guss
DB_USER=postgres
DB_PASSWORD=password
NODE_ENV=development
FORCE_SYNC=false
AUTO_CONNECT_DB=true
```

## 🚀 Использование

### Импорт в проекте
```typescript
import { 
  databaseManager, 
  User, 
  Round, 
  Participant,
  sequelize 
} from 'the-last-of-guss-database';
```

### Подключение к базе данных
```typescript
// Автоматическое подключение при импорте
await databaseManager.connect();

// Или ручное подключение
await databaseManager.connect();
console.log(databaseManager.getConnectionStatus()); // true

// Проверка здоровья БД
const health = await databaseManager.healthCheck();
console.log(health.status); // 'healthy'
```

### Примеры использования моделей

#### Создание пользователя
```typescript
const user = await User.create({
  username: 'testuser',
  password: 'securepassword'
});
```

#### Создание раунда
```typescript
const round = await Round.create({
  start_date: new Date('2024-01-01T10:00:00Z'),
  end_date: new Date('2024-01-01T11:00:00Z')
});
```

#### Участие в раунде
```typescript
const participant = await Participant.create({
  user_id: user.id,
  round_id: round.id,
  tap_count: 100,
  score: 150
});
```

## 📊 Скрипты

```bash
# Сборка проекта
npm run build

# Сборка в watch-режиме
npm run build:watch

# Разработка с watch-режимом
npm run dev

# Очистка dist директории
npm run clean
```

## 🔄 Миграции

Для управления структурой БД используются миграции Sequelize. Миграции находятся в основном backend пакете и выполняются через корневой package.json:

```bash
# Из корня монопакета
npm run migrate:up
```

## 🛠️ Разработка

### Структура экспорта
Пакет предоставляет следующие точки входа:

- Основной экспорт: `import { User, Round } from 'the-last-of-guss-database'`
- Конфигурация: `import { sequelize } from 'the-last-of-guss-database/config'`
- Модели: `import { User } from 'the-last-of-guss-database/models'`
- Типы: `import type { UserAttributes } from 'the-last-of-guss-database/types'`

### Сборка
Сборка включает:
- Компиляцию TypeScript
- Создание index.js файлов для директорий
- Исправление импортов в скомпилированных файлах
- Копирование необходимых файлов

## 🐛 Отладка

Включите логирование запросов в режиме разработки:
```env
NODE_ENV=development
```

Для принудительной синхронизации моделей (только для разработки):
```env
FORCE_SYNC=true
```

## 📝 Примечания

- Пароли автоматически хешируются с bcrypt (12 раундов)
- Роли определяются автоматически по имени пользователя
- Статусы раундов рассчитываются автоматически на основе дат
- Все модели используют UUID в качестве первичных ключей
- Временные метки создаются автоматически

## 🔒 Безопасность

- Пароли никогда не возвращаются в методах toJSON()
- Все запросы используют параметризованные запросы через Sequelize
- Валидация данных на уровне модели и БД
